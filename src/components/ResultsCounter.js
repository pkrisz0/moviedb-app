import React from 'react';

export default ({ count }) => (
  <div style={{ width: '100px', display: 'inline-block', margin: '10px' }}>
    <p>
      <strong>{count}</strong>
        MOVIES
    </p>
  </div>
);
